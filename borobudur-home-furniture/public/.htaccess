# SPA routing for Apache/cPanel: serve real files if they exist,
# otherwise fall back to index.html so React Router handles the URL.
RewriteEngine On
RewriteRule ^index\.html$ - [L]
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . index.html [L]
